let obj = {
    'chave1': 1,
    'chave2': 2,
    'chave3': 3,
}

console.log(obj); // Object { chave1: 1, chave2: 2, chave3: 3 }

console.log(Object.keys(obj)); // Array(3) [ "chave1", "chave2", "chave3" ]
